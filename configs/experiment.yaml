# Reference note only.
#
# The live control path is:
# - pipeline/run_experiments.py
# - slurm/00_preamble.sh
# - the MEDS tokenizer configs in ../fms-ehrs/fms_ehrs/config/

reference_only: true

counts:
  exp1_primary: 12
  exp2_primary: 6
  exp2_auxiliary_xval_affine: 2
  exp3_primary: 4
  total_primary: 22
  total_reported: 24

paths:
  tokenized_root: artifacts/runs/tokenized/
  model_root: artifacts/runs/models/
  exp3_root: artifacts/runs/exp3/
  stats_root: artifacts/runs/statistics/paper_audit_20260316_idaligned_fullstats/

active_configs:
  meds_default: ../fms-ehrs/fms_ehrs/config/mimic-meds-ed.yaml
  meds_exp3_icu: ../fms-ehrs/fms_ehrs/config/mimic-meds-exp3-icu.yaml

notes:
  - Use pipeline/run_experiments.py to generate jobfiles (root run_experiments.py is a compatibility wrapper).
  - Use pipeline/scripts/regenerate_aligned_family_stats.py to refresh the paper stats root.
