# Archived CLIF Validation Helpers

These scripts were kept for historical parity checks against CLIF-side tables.

They are not part of the live benchmark run path. The active benchmark now uses the MEDS-only path plus the Exp3 semantic-arm construction under `scripts/`.
